server:
  port: 8081

spring:
  application:
    name: credit
  datasource:
    username: "sebastian"
    password: "password"
    url: jdbc:postgresql://postgres:5432/credit
  jpa:
      hibernate:
        ddl-auto: update
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
          format_sql: true
      show-sql: true