server:
  port: 8083

spring:
  application:
    name: product
  datasource:
    username: "sebastian"
    password: "password"
    url: jdbc:postgresql://postgres:5432/product
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true