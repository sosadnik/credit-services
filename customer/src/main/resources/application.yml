server:
  port: 8082

spring:
  application:
    name: customer
  datasource:
    username: "sebastian"
    password: "password"
    url: jdbc:postgresql://postgres:5432/customer
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true